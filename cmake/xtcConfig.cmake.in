@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find OpenMP if it was used during build
if(@XTC_USE_OPENMP@)
    find_dependency(OpenMP)
endif()

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/xtcTargets.cmake")

check_required_components(xtc)
